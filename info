Datos:
    no se puede usar dos librerias en el mismo servidor.
    app es el estandar para asignar como variable de una libreria.
    urlencoded decodifica el cuerpo de los .post.
    el controlador procesa datos que recibe el servidor.
    middleware es una funcion que procesa los datos para.
    protocolo commonjs.
    el scope de node es solo los archivos y carpetas hermanas del archivo indice.
    app.use(express.urlencoded({extended: false})) sirve para que se puedan leer los post.

buscar:
    servicios.
    utilidades.
    vistas.